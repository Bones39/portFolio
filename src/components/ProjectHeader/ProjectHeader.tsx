import styles from "./ProjectHeader.module.css";

const ProjectHeader = () => {
	return <h2 className={styles.ProjectHeader}>Projects (en anglais car on est des devs)</h2>
}
export default ProjectHeader;